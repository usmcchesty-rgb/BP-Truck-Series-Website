// Fantasy page — frontend only (coming soon).
// No backend calls, auth, scoring, or participant login yet.

window.BPFantasyComingSoon = {
  init() {
    // Placeholder hook if we later add dynamic announcements without backend changes.
  },
};

window.addEventListener('DOMContentLoaded', () => {
  try {
    window.BPFantasyComingSoon?.init?.();
  } catch {
    // No-op
  }
});

